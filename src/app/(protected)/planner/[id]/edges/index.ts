import type { Edge, EdgeTypes } from "@xyflow/react";

export const initialEdges = [
    // { id: "el-1", source: "1", target: "2", animated: true },
] satisfies Edge[];

export const edgeTypes = {} satisfies EdgeTypes;
